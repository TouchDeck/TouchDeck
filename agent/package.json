{
  "name": "pideck-agent",
  "version": "0.1.0",
  "main": "src/index.ts",
  "scripts": {
    "start": "ts-node-dev --respawn src/index.ts",
    "build:tsc": "rimraf build && tsc",
    "build:nexe": "rimraf exe && nexe build/index.js -o exe/${npm_package_name}-${npm_package_version} -t ${npm_package_config_nexe_target_version}",
    "build:nexe:linux": "nexe build/index.js -o exe/${npm_package_name}-${npm_package_version}-linux -t linux-x64-${npm_package_config_nexe_target_version}",
    "build:nexe:windows": "nexe build/index.js -o exe/${npm_package_name}-${npm_package_version}-windows.exe -t windows-x64-${npm_package_config_nexe_target_version}",
    "build:nexe:mac": "nexe build/index.js -o exe/${npm_package_name}-${npm_package_version}-mac -t mac-x64-${npm_package_config_nexe_target_version}",
    "build:nexe:all": "rimraf exe && run-p build:nexe:linux build:nexe:windows build:nexe:mac",
    "build": "npm run build:tsc && npm run build:nexe:all",
    "prettier": "prettier --write \"src/**/*.(ts)\"",
    "lint": "eslint . --ext .ts --fix",
    "format": "npm run prettier && npm run lint"
  },
  "config": {
    "nexe_target_version": "12.16.2"
  },
  "dependencies": {
    "@luca_scorpion/tinylogger": "^0.1.0",
    "express": "^4.17.1",
    "node-fetch": "^2.6.1",
    "obs-websocket-js": "^4.0.1",
    "reflect-metadata": "^0.1.13",
    "uuid": "^8.3.0",
    "ws": "^7.3.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.7",
    "@types/express": "^4.17.7",
    "@types/node": "^14.6.0",
    "@types/node-fetch": "^2.5.7",
    "@types/uuid": "^8.3.0",
    "@types/ws": "^7.2.7",
    "@typescript-eslint/eslint-plugin": "^3.9.1",
    "@typescript-eslint/parser": "^3.9.1",
    "eslint": "^7.7.0",
    "eslint-config-airbnb-typescript": "^9.0.0",
    "eslint-config-prettier": "^6.11.0",
    "eslint-plugin-import": "^2.22.0",
    "nexe": "^3.3.7",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.0.5",
    "rimraf": "^3.0.2",
    "ts-node": "^8.10.2",
    "ts-node-dev": "^1.0.0-pre.57",
    "typescript": "^3.9.7"
  },
  "eslintConfig": {
    "plugins": [
      "@typescript-eslint"
    ],
    "extends": [
      "airbnb-typescript/base",
      "plugin:@typescript-eslint/recommended",
      "prettier",
      "prettier/@typescript-eslint"
    ],
    "parserOptions": {
      "project": "./tsconfig.json"
    },
    "rules": {
      "no-plusplus": "off",
      "no-restricted-syntax": "off",
      "@typescript-eslint/ban-ts-comment": "off"
    }
  },
  "prettier": {
    "singleQuote": true,
    "endOfLine": "auto"
  }
}
